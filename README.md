# URL Shortener API

A REST API built with Python and Flask to shorten URLs.

## Endpoints
- POST /shorten → Shortens a long URL
- GET /<code> → Redirects to original URL  
- GET /all → Returns all stored URLs

## Tech Stack
Python, Flask, REST API, Git

## How to Run
pip install flask
python app.py